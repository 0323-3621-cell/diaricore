__pycache__/
*.py[cod]
.venv/
venv/
diaricore.db
diaricore.local.db
.env
*.log

# Model weights — stored on HuggingFace Hub, not in git
model/pytorch_model.bin
model/onnx_export/

# Jupyter checkpoints
.ipynb_checkpoints/

# Local clones / nested repos (keep outside git)
hf_space_clone/

# Notebook scratch exports (not used by runtime)
model/nb_*.txt

# ML experiment notebooks (keep out of app repo by default)
ml-service/*KeyBERT*.ipynb
